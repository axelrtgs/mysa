__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.env
# raw captures carry device ids, serials and names; only processed
# samples are shareable
docs/samples/raw/

# session caches; the default lives outside the repo, this covers --session overrides
*session.json
.pymysa/
